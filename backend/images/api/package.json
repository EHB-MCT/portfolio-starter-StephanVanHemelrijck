{
  "name": "build",
  "version": "1.0.0",
  "description": "Portfolio API for Development 5, 2023-2024, Erasmushogeschool Brussels",
  "main": "index.js",
  "scripts": {
    "test-watch": "jest --watchAll --detectOpenHandles",
    "test": "jest --detectOpenHandles",
    "start:express": "node index.js",
    "start": "npm run migrate && npm run test && node src/index.js",
    "dev": "npm run migrate && npm run seed && nodemon -L src/index.js",
    "migrate": "npx knex migrate:latest",
    "seed": "npx knex seed:run"
  },
  "author": "Stephan Van Hemelrijck",
  "license": "ISC",
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "dotenv": "^16.3.1",
    "ejs": "^3.1.9",
    "express": "^4.18.2",
    "knex": "^3.1.0",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "assert": "^2.1.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
